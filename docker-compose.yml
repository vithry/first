version: '3'

services:
  web:
    image: nginx
    container_name: web
    ports:
      - "8080:80"
    volumes:
      - ./site:/usr/share/nginx/html
      - ./site/config/nginx.conf:/etc/nginx/nginx.conf
      - ./site/config/conf.d:/etc/nginx/conf.d
